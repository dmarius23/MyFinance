# ---- OS / editor ----
.DS_Store
~$*
*.swp
.idea/
.vscode/
*.iml

# ---- Java / Maven (backend) ----
target/
build/
*.class
*.jar
*.war
.mvn/
!.mvn/wrapper/maven-wrapper.jar

# ---- Node / Vite (frontend) ----
node_modules/
dist/
dist-ssr/
dev-dist/
*.local
.vite/
coverage/
*.tsbuildinfo

# ---- Env / secrets ----
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# ---- Logs / tmp ----
*.log
logs/
tmp/

# ---- Supabase ----
.supabase/

# Real bank-statement samples (PII) — never commit; redacted text fixtures live in test resources
docs/samples/

# Real ANAF declaration PDFs (PII: CNP, salaries) — never commit; tests skip when absent
backend/src/test/resources/fixtures/anaf/

# Local dev launchers (machine-specific env)
*.local.sh
backend/run-local.sh
